{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "git add -A; git diff --cached --quiet || git commit -m 'auto: update files'; git push origin main 2>/dev/null || true",
            "statusMessage": "GitHub에 업로드 중...",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
